// Generated by CoffeeScript 1.6.1
(function() {
  var ALARM_NAME, BLINK_INTERVAL, BLINK_LIMIT, Chromagochi, LIFE_MINUTES, MAX_HUE, Pet, STORAGE_KEY, TICK_MINUTES, cg, hsl2rgb,
    _this = this;

  TICK_MINUTES = 1;

  ALARM_NAME = 'tick';

  STORAGE_KEY = 'chromagochi';

  LIFE_MINUTES = 120;

  BLINK_INTERVAL = 300;

  BLINK_LIMIT = 30;

  MAX_HUE = 359;

  hsl2rgb = function(H, S, L) {
    var B, B1, C, G, G1, H_, R, R1, X, m;
    C = (1 - Math.abs((2 * L) - 1)) * S;
    H_ = H / 60;
    X = C * (1 - Math.abs((H_ % 2) - 1));
    if (H === void 0 || isNaN(H) || H === null) {
      R1 = G1 = B1 = 0;
    } else {
      if (H_ >= 0 && H_ < 1) {
        R1 = C;
        G1 = X;
        B1 = 0;
      } else if (H_ >= 1 && H_ < 2) {
        R1 = X;
        G1 = C;
        B1 = 0;
      } else if (H_ >= 2 && H_ < 3) {
        R1 = 0;
        G1 = C;
        B1 = X;
      } else if (H_ >= 3 && H_ < 4) {
        R1 = 0;
        G1 = X;
        B1 = C;
      } else if (H_ >= 4 && H_ < 5) {
        R1 = X;
        G1 = 0;
        B1 = C;
      } else if (H_ >= 5 && H_ < 6) {
        R1 = C;
        G1 = 0;
        B1 = X;
      }
    }
    m = L - (C / 2);
    R = (R1 + m) * 255;
    G = (G1 + m) * 255;
    B = (B1 + m) * 255;
    R = Math.round(R);
    G = Math.round(G);
    B = Math.round(B);
    return {
      R: R,
      G: G,
      B: B
    };
  };

  Pet = (function() {

    Pet.prototype["default"] = {
      name: 'Chromagochi',
      happiness: LIFE_MINUTES
    };

    Pet.prototype.blinker = null;

    Pet.prototype.status = null;

    function Pet(callback) {
      var self,
        _this = this;
      this.stopBlinking = function() {
        return Pet.prototype.stopBlinking.apply(_this, arguments);
      };
      this.startBlinking = function() {
        return Pet.prototype.startBlinking.apply(_this, arguments);
      };
      this.load = function(callback) {
        return Pet.prototype.load.apply(_this, arguments);
      };
      this.update = function() {
        return Pet.prototype.update.apply(_this, arguments);
      };
      this.age = function(minutes) {
        return Pet.prototype.age.apply(_this, arguments);
      };
      this.feed = function() {
        return Pet.prototype.feed.apply(_this, arguments);
      };
      self = this;
      this.load(function() {
        if (callback != null) {
          callback();
        }
        return self.update();
      });
    }

    Pet.prototype.isAlive = function() {
      return this.status.happiness > 0;
    };

    Pet.prototype.feed = function() {
      window._gaq.push(['_trackEvent', this.status.happiness, 'feed']);
      this.status.happiness = this["default"].happiness;
      this.save();
      this.stopBlinking();
      return this.update();
    };

    Pet.prototype.age = function(minutes) {
      this.status.happiness--;
      this.save();
      this.update();
      if (this.status.happiness === BLINK_LIMIT) {
        return this.startBlinking();
      }
    };

    Pet.prototype.update = function() {
      var c, h;
      chrome.browserAction.setBadgeText({
        text: this.status.happiness + ''
      });
      if (this.blinker == null) {
        h = Math.round(((this.status.happiness - BLINK_LIMIT) / (LIFE_MINUTES - BLINK_LIMIT)) * MAX_HUE);
        h = (30 + h) % MAX_HUE;
        c = hsl2rgb(h, 1, 0.8);
        return this._setColor([c.R, c.G, c.B, 255]);
      }
    };

    Pet.prototype.clone = function(object) {
      return JSON.parse(JSON.stringify(object));
    };

    Pet.prototype.load = function(callback) {
      var self;
      self = this;
      chrome.storage.local.get(STORAGE_KEY, function(items) {
        self.status = items[STORAGE_KEY] || self.clone(self["default"]);
        if (callback != null) {
          return callback();
        }
      });
      return true;
    };

    Pet.prototype.save = function(callback) {
      var data;
      data = {};
      data[STORAGE_KEY] = this.status;
      return chrome.storage.local.set(data, function() {
        if (callback != null) {
          return callback();
        }
      });
    };

    Pet.prototype._setColor = function(color) {
      chrome.browserAction.setBadgeBackgroundColor({
        color: color
      });
      return true;
    };

    Pet.prototype.startBlinking = function() {
      var self, toggle;
      self = this;
      toggle = false;
      return this.blinker = setInterval(function() {
        var color;
        color = [0, 0, 0, 255];
        if (toggle) {
          color = [255, 0, 0, 255];
        }
        self._setColor(color);
        return toggle = !toggle;
      }, BLINK_INTERVAL);
    };

    Pet.prototype.stopBlinking = function() {
      if (this.blinker != null) {
        clearInterval(this.blinker);
      }
      this.blinker = null;
      return true;
    };

    return Pet;

  })();

  Chromagochi = (function() {

    Chromagochi.prototype.pet = null;

    function Chromagochi() {
      var self,
        _this = this;
      this.processAlarm = function(alarm) {
        return Chromagochi.prototype.processAlarm.apply(_this, arguments);
      };
      self = this;
      this.pet = new Pet(null);
      chrome.browserAction.onClicked.addListener(function(tab) {
        return self.pet.feed();
      });
      chrome.alarms.onAlarm.addListener(this.processAlarm);
      true;
    }

    Chromagochi.prototype.start = function() {
      chrome.alarms.create(ALARM_NAME, {
        periodInMinutes: TICK_MINUTES
      });
      return true;
    };

    Chromagochi.prototype.stop = function() {
      chrome.alarms.clear(ALARM_NAME);
      return true;
    };

    Chromagochi.prototype.processAlarm = function(alarm) {
      if (alarm.name === ALARM_NAME) {
        if (this.pet.isAlive()) {
          return this.pet.age(alarm.periodInMinutes);
        }
      }
    };

    return Chromagochi;

  })();

  (function(global) {
    global._gaq = global._gaq || [];
    global._gaq.push(['_setAccount', 'UA-115818-75']);
    global._gaq.push(['_trackPageview']);
    return (function() {
      var ga, s;
      ga = document.createElement("script");
      ga.type = "text/javascript";
      ga.async = true;
      ga.src = "https://ssl.google-analytics.com/ga.js";
      s = document.getElementsByTagName("script")[0];
      s.parentNode.insertBefore(ga, s);
    })();
  })(window);

  cg = new Chromagochi;

  cg.start();

}).call(this);
